/*
* Package validator implements various validations tools to ensure that values
* match your requirements. Package validator aims to allow generic validation
* and to allow composable validations via a common interface.
 */
package validator
